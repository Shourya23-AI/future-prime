info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login
  body:
    type: json
    data: |-
      {"email": "admin@futureprime.com", "password": "Admin@1234"}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
